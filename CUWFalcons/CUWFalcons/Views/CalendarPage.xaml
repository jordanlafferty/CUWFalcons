<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:XamForms.Controls;assembly=XamForms.Controls.Calendar"  
             x:Class="CUWFalcons.Views.CalendarPage"
             Title="Calendar"
             BackgroundColor="White">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add an Event"
                     Clicked="addEventToolbar_Clicked">
        </ToolbarItem>

    </ContentPage.ToolbarItems>
    <StackLayout Spacing="20" Padding="15">
        <controls:Calendar Padding="10,0,10,0"   
                   SelectedBorderWidth="4"   
                   DisabledBorderColor="Black"  
                   ShowNumberOfWeek="false"  
                   StartDay="Sunday"  
                   TitleLabelTextColor="Gray"  
                   TitleLeftArrowTextColor="#245699"
                   TitleRightArrowTextColor="#245699"
                   SelectedDate="{Binding Date}"  
                   SpecialDates="{Binding Attendances}"  
                   DateCommand="{Binding DateChosen}"/>
        <Label Text="Click on a Date to See a List of Events" FontSize="Medium" HorizontalTextAlignment="Center" TextColor="#B1B3B3" />

    </StackLayout>   
</ContentPage>
